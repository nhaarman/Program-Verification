/**
 * A class to box int values.
 */
class LockableInteger {
  int val;

  resource state() = Perm(val,1);

  //resource lock_invariant() = Perm(val,1);

  ensures state() ** this.val == val;
  LockableInteger(int val) {
    this.val = val;
    fold state();
  }
}
